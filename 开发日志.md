# 开发日志

## 2026-06-22

- 修复远程订阅导入重复判断：节点 fingerprint 去重范围改为当前目标分组，允许不同分组保存相同上游节点。
- 修复上游 Mihomo 模板保存名称：自动保存模板时加入目标分组名，避免只显示 `Upstream Mihomo <digest>` 无法区分来源分组。
- 修复 Mihomo 导出重复代理名：导出前自动将重复 `proxies[].name` 改为 `原名 #2/#3`，并同步展开 `proxy-groups[].proxies` 引用，避免 Mihomo 报 `duplicate name`。
- 追加修复非上游模板导出路径：Mihomo YAML 保存模板前同样执行代理名去重和代理组引用同步，避免保存后的模板内容本身携带重复 `proxies[].name`。
- 验证：`cargo fmt` 通过；`docker build --build-arg DOCKER_REGISTRY=docker.m.daocloud.io --target builder -f backend/Dockerfile -t sublinkx-rs-backend-builder:check .` 通过；`docker run --rm sublinkx-rs-backend-builder:check cargo test mihomo_proxy_name -- --nocapture` 通过；`git diff --check` 通过。
- 追加验证：`docker run --rm sublinkx-rs-backend-builder:check cargo test sanitizes_saved_mihomo_template_duplicate_proxy_names -- --nocapture` 通过。
- 编译：使用 `docker build --build-arg DOCKER_REGISTRY=docker.m.daocloud.io` 完成生产镜像编译；后端镜像 `jojhaa/sublinkx-rs-backend:latest`，镜像 ID `987b70fc4352`，大小 `150MB`；前端镜像 `jojhaa/sublinkx-rs-frontend:latest`，镜像 ID `c8457659f56b`，大小 `93.2MB`。
- 发布：`docker push docker.io/jojhaa/sublinkx-rs-backend:latest` 成功，远端 digest `sha256:987b70fc4352d403d54ae4488f57565290b228c1e9ae18a82ccb285960dc2f76`；`docker push docker.io/jojhaa/sublinkx-rs-frontend:latest` 成功，远端 digest `sha256:c8457659f56bbcf1e8e3a032252a5f57b33b5a7573d3be709ef13d2b5a9abe06`。
- 说明：Docker CLI 不支持直接删除 Docker Hub 远端 tag，本次采用覆盖推送更新 `latest`，避免删除后再上传期间远端镜像不可拉取。
- 配置修复：本地 `.env` 将 `JWT_SECRET` 替换为 32 字符以上随机值，将首次引导密码替换为非默认随机值，并补齐 `MYSQL_PASSWORD`/`MYSQL_ROOT_PASSWORD` 以通过 compose 插值校验。
- 配置验证：`docker compose config --quiet` 通过。
- 功能修复：Mihomo/Clash 导出会把模板中的 `include-all-proxies: true` 展开为显式节点列表，并在模板已有 `AUTO` 组时不再追加重复 `AUTO` 组，避免 178 个已选节点在客户端只显示少量节点。
- 部署修复：前端 Nginx 增加 Docker DNS resolver 和运行时 upstream 变量，避免 `backend` 服务未就绪时启动失败。
- 验证：`docker run --rm -v ${PWD}/frontend/nginx.conf:/etc/nginx/conf.d/default.conf:ro docker.m.daocloud.io/library/nginx:1.29-alpine nginx -t` 通过；`docker run --rm sublinkx-rs-backend-builder:check cargo test include_all_proxy_groups -- --nocapture` 通过。
- 说明：本机 `cargo check` 受限于缺少 MSVC `link.exe`，未能进入项目代码编译阶段；已用 Docker/Linux 构建路径完成验证。
- 编译发布：重新构建并推送 Docker Hub `latest` 镜像；后端镜像 ID `3ca280fae582`，大小 `150MB`，远端 digest `sha256:3ca280fae582589881c102820fc2e0b4c91247618d5438f397f0cb2df38b44d7`；前端镜像 ID `c16104f11122`，大小 `93.2MB`，远端 digest `sha256:c16104f11122516aef809d01abd0074afcbcfe654a5a271dab62da5912aed89c`。
- 修复 Mihomo 普通导出器测速失败：无上游模板时现在生成 `PROXY` select 组和 `AUTO` url-test 组，`AUTO` 不再错误输出为 `select`；已有模板规则不再被强行追加 `MATCH,AUTO`。
- 调整默认测速 URL：Mihomo 导出器、内置模板、后端默认设置和前端占位值统一使用 `https://cp.cloudflare.com/generate_204`；新增迁移仅将仍为旧默认 `https://www.gstatic.com/generate_204` 的设置和内置 Clash/Mihomo 模板替换为新默认，不覆盖用户自定义 URL。
- 验证：`cargo fmt` 通过；`docker build --build-arg DOCKER_REGISTRY=docker.m.daocloud.io --target builder -f backend/Dockerfile -t sublinkx-rs-backend-builder:check .` 通过；`docker run --rm sublinkx-rs-backend-builder:check cargo test mihomo -- --nocapture` 通过；`backend\mihomo\mihomo.exe -t` 对新 `PROXY + AUTO url-test` 配置校验通过；`npm run build` 通过；`git diff --check` 通过。

## 2026-06-23

- 定位 Mihomo 导出节点全部不可用原因：用户提供的 `测试.yaml` 触发 `GEOIP,CN` 和 DNS `fallback-filter` 的 MMDB 初始化，当前环境无法下载 `geoip.metadb` 时 Mihomo 整份配置启动失败；原始 `iNetS` 可用是因为使用 `RULE-SET`/`no-resolve` 规则，没有强依赖 GeoIP 数据库。
- 修复内置 Clash/Mihomo 路由模板：移除默认模板中的 `dns.fallback`、`fallback-filter` 和 `GEOIP,CN` 规则；更新模板文档；SQLite 迁移和 MySQL 启动升级会清理旧内置模板中的同类默认片段。
- 验证：`backend\mihomo\mihomo.exe -t` 对移除 fallback/GeoIP 后的用户配置校验通过；`docker run --rm sublinkx-rs-backend-builder:check cargo test built_in_clash_template_avoids_geoip_database_dependency -- --nocapture` 通过。
- 补全默认导出规则：Clash 内置模板扩展为 ACL4SSR 风格常用分流，Mihomo 内置模板扩展为接近 iNetS 的 MetaCubeX `.mrs` rule-provider 分流，覆盖 AI、GitHub、YouTube、Google、Telegram、Netflix、Bilibili、Spotify、Steam、PayPal、Microsoft、Apple、国内直连和兜底策略。
- 修复无模板 Clash 导出：默认模板提取逻辑增加中文标题匹配，避免实际取到“最小分流模板”；文档完整模板补齐 `MATCH,漏网之鱼`，并统一使用 `https://cp.cloudflare.com/generate_204`。
- 兼容旧库：后端启动时会仅升级仍停留在旧极简内容、且没有 `rule-providers` 的内置 Clash/Mihomo 模板；用户自定义模板不覆盖。
- 验证：新的 Mihomo 内置模板加 dummy 节点后通过 `backend\mihomo\mihomo.exe -t`；文档完整 Clash 模板加 dummy 节点后通过 `backend\mihomo\mihomo.exe -t`；`cargo fmt`、Docker builder 目标单测和 `git diff --check` 通过。
- 编译发布：使用 `docker build --build-arg DOCKER_REGISTRY=docker.m.daocloud.io` 完成生产镜像编译并推送 Docker Hub `latest`；后端镜像 `jojhaa/sublinkx-rs-backend:latest`，镜像 ID `31f52aa551e9`，大小 `150MB`，远端 digest `sha256:31f52aa551e9f681a1b5fecbc43679c721c33ed92b203ebd19c7770a34de185a`；前端镜像 `jojhaa/sublinkx-rs-frontend:latest`，镜像 ID `3753cdb6382d`，大小 `93.2MB`，远端 digest `sha256:3753cdb6382d41a2c6ee46a07f2df25d25f90a883600761e950e266b735e4daf`。
- 部署修复：分析前端 Nginx 日志确认 `backend could not be resolved` 是 Docker DNS 中没有健康后端服务导致；后端镜像加入 `curl`，Compose 为后端增加 `/healthz` 健康检查，并让前端 `depends_on` 等待 `service_healthy`，减少后端启动失败时前端直接暴露 502 的情况。
- 文档：中英文 Docker 部署文档补充 `backend could not be resolved` 排查方式，优先执行 `docker compose ps` 和 `docker compose logs --tail=200 backend`。
- 编译发布：重新构建并推送后端镜像 `jojhaa/sublinkx-rs-backend:latest`；镜像 ID `912e2d96e9e2`，大小 `158MB`，远端 digest `sha256:912e2d96e9e251e2f41e11d5c99a452061da056d9262c3283489dfd1fd6acd68`；`docker compose config --quiet` 和 `git diff --check` 通过。
- 部署修复：恢复历史迁移 `0006_latency_real_link_settings.sql` 原始内容，解决已执行过第 6 号迁移的线上 SQLite 数据库在新镜像启动时报 `Migrate(VersionMismatch(6))`；测速 URL 迁移继续由 `0012_latency_test_url_default.sql` 负责，不修改历史迁移校验。
- 编译发布：重新构建并推送后端镜像 `jojhaa/sublinkx-rs-backend:latest`；镜像 ID `84e71d866a83`，大小 `158MB`，远端 digest `sha256:84e71d866a8351b25a0fac780185532afa8eeead8adf128bc9f666a7838d2cc8`；`git diff --check` 通过。
- 发布补充：为绕过部署端 `latest` 缓存问题，额外推送后端不可变标签 `docker.io/jojhaa/sublinkx-rs-backend:20260623-migrate-fix`，远端 digest `sha256:84e71d866a8351b25a0fac780185532afa8eeead8adf128bc9f666a7838d2cc8`。
- 发布准备：后端、前端和锁文件版本号更新到 `0.1.1`；`CHANGELOG.md` 新增 `v0.1.1 - 2026-06-23`，整理本轮安全加固、Mihomo/Clash 导出、延迟测试、订阅二维码和 Docker 部署修复说明。

## 2026-07-08

- 修复上游订阅导入 Shadowsocks 节点缺失：Mihomo YAML `type: ss`/`shadowsocks` 现在会转换为 SIP002 `ss://` 链接后进入统一节点解析流程。
- 增强 Shadowsocks URI 解析：支持 SIP002 `userinfo` Base64、尾部 `/`、`udp` query 和 `plugin` query，避免常见上游格式解析失败。
- 保留 SS 再导出字段：Mihomo 导出会带回已导入 SS 节点的 `udp` 和 `plugin` 设置，减少导入后再导出的配置丢失。
- 验证：`cargo fmt --manifest-path backend\Cargo.toml` 通过；`cargo test --manifest-path backend\Cargo.toml shadowsocks -- --nocapture` 通过，3 个相关单测全部通过。
- 功能增强：新增上游订阅链接管理页面 `/upstreams`，侧边栏增加“上游订阅”入口；页面支持保存、编辑、删除和手动重新导入上游订阅链接，并展示最近导入状态、导入统计、目标节点分组和保存的上游模板。
- 后端增强：新增 `upstream_subscriptions` 表、SQLite 迁移 `0013_upstream_subscriptions.sql`、MySQL 初始化表结构、上游订阅管理 API；旧节点页上游导入成功后会自动 upsert 到管理表，列表接口会从已有节点 `source_ref` 反向补齐历史上游链接。
- 验证追加：`npm --prefix frontend run build` 通过；`cargo test --manifest-path backend\Cargo.toml` 通过，47 个后端单测全部通过；`git diff --check` 通过。
- 发布准备：后端、前端和锁文件版本号更新到 `0.1.2`；`CHANGELOG.md` 新增 `v0.1.2 - 2026-07-08`，整理上游订阅管理和 Shadowsocks 上游导入修复说明。
- Docker 发布：Docker Desktop 初始未运行，已启动 Docker 服务后使用 `DOCKER_REGISTRY=docker.m.daocloud.io` 构建并推送 `0.1.2` 镜像。
- 后端镜像已推送到 Docker Hub：`docker.io/jojhaa/sublinkx-rs-backend:latest`、`docker.io/jojhaa/sublinkx-rs-backend:0.1.2`、`docker.io/jojhaa/sublinkx-rs-backend:v0.1.2`，远端 digest 均为 `sha256:1e3f20b4dff0de8456a10a1beb173934f2251d36b920fdd960da51566bf2e52b`。
- 前端镜像已推送到 Docker Hub：`docker.io/jojhaa/sublinkx-rs-frontend:latest`、`docker.io/jojhaa/sublinkx-rs-frontend:0.1.2`、`docker.io/jojhaa/sublinkx-rs-frontend:v0.1.2`，远端 digest 均为 `sha256:2394535480207dc98cd9d4d795b80812dd325d4d1e433e893e560e832b0f1983`。
- 发布验证：`docker buildx imagetools inspect docker.io/jojhaa/sublinkx-rs-backend:0.1.2` 和 `docker buildx imagetools inspect docker.io/jojhaa/sublinkx-rs-frontend:0.1.2` 均可读取远端镜像 digest。
- 前端优化：上游订阅管理页移除原四列表格展示，改为专用卡片列表；订阅 URL 仅显示域名和单行短摘要，完整链接保留在悬停提示中；最近导入状态、导入统计、上游模板和操作按钮被收敛到固定信息区，解决长链接导致页面杂乱和竖排的问题。
- 验证：`npm --prefix frontend run build` 通过。
- 功能调整：托管上游订阅取消手动选择节点分组；后端创建、更新和重新导入上游订阅时会自动按订阅名称创建或复用同名节点分组，并忽略旧客户端传入的 `group_id`；前端编辑弹窗改为展示“按订阅名称自动分组”的说明。
- 验证：`cargo fmt --manifest-path backend\Cargo.toml`、`cargo test --manifest-path backend\Cargo.toml`、`npm --prefix frontend run build` 均通过。
- Docker 发布：使用 `DOCKER_REGISTRY=docker.m.daocloud.io` 重新构建并推送后端、前端 `latest`、`0.1.2`、`v0.1.2` 标签；后端远端 digest 为 `sha256:8a8224ea832fb588cd6b0488f0fe2545ead6c484b991fe1550fe896031486a24`，前端远端 digest 为 `sha256:a3694fcb24d90566c18f0490c8b763299ffd6e2b01d26b730543383d7a926402`。
- 发布验证：`docker buildx imagetools inspect docker.io/jojhaa/sublinkx-rs-backend:0.1.2` 和 `docker buildx imagetools inspect docker.io/jojhaa/sublinkx-rs-frontend:0.1.2` 均可读取远端镜像 digest。
## 2026-07-09

- 缺陷修复：上游订阅删除后会被列表接口从已导入节点的 `source_ref` 自动回填，导致页面表现为“无法删除”；删除上游订阅时现在会保留节点本体，但将相关节点来源改回 `manual` 并清空 `source_ref`，防止再次回填。
- 前端说明：删除确认文案补充“已导入节点不会被删除，但会解除来源关联”。
- 验证：`cargo fmt --manifest-path backend\Cargo.toml`、`cargo test --manifest-path backend\Cargo.toml`、`npm --prefix frontend run build` 均通过。
- 功能增强：删除上游订阅时新增可选“同时删除相关节点”；后端 `DELETE /api/v1/upstream-subscriptions/{id}?delete_nodes=true` 会删除仍关联该上游链接的节点，默认行为保持为仅解除来源关联；接口响应返回 `deleted_nodes` 和 `detached_nodes` 便于前端提示。
- 前端交互：删除上游订阅时先确认删除链接，再二次确认是否同时删除相关节点；选择取消第二步时仅解除来源关联。
- 验证：`cargo fmt --manifest-path backend\Cargo.toml`、`cargo test --manifest-path backend\Cargo.toml`、`npm --prefix frontend run build` 均通过。
- 模板优化：Mihomo/Clash 默认模板的“节点选择”改为手动选择优先，再进入自动测速选择；无上游模板的 Mihomo 导出同步生成 `PROXY`、`MANUAL`、`AUTO` 三组，客户端默认不再落到自动选择。
- 规则增强：内置模板补充 AI、GitHub、YouTube、Netflix、Telegram、社媒流媒体和游戏平台等常用站点的显式规则；新增迁移 `0014_mihomo_template_manual_first_rules.sql`，用于更新仍停留在内置模板内容的旧库记录，不覆盖用户自定义模板。
- 验证：`cargo fmt --manifest-path backend\Cargo.toml`、`cargo test --manifest-path backend\Cargo.toml`、`npm --prefix frontend run build`、`git diff --check` 均通过。
- 发布准备：后端、前端和锁文件版本号更新到 `0.1.3`；`CHANGELOG.md` 新增 `v0.1.3 - 2026-07-09`，整理上游订阅管理、Mihomo/Clash 模板手动优先、常用站点规则和导出兼容性修复说明。
- Docker 发布：使用 `DOCKER_REGISTRY=docker.m.daocloud.io` 构建并推送后端、前端 `latest`、`0.1.3`、`v0.1.3` 标签；后端远端 digest 为 `sha256:5d32315ddb3c509707dd5c74a6c858be05ebb4f6a2ef0972fa3554111107390d`，前端远端 digest 为 `sha256:dcd1c73a4cef8f6f113957173b6be275280490b7d484925cd397b48394810556`。
- 发布验证：`docker buildx imagetools inspect docker.io/jojhaa/sublinkx-rs-backend:0.1.3` 和 `docker buildx imagetools inspect docker.io/jojhaa/sublinkx-rs-frontend:0.1.3` 均可读取远端镜像 digest。
- 发布排障：推送初始失败原因是 Docker Desktop 内部 DNS 将 Docker Hub 解析到异常地址；通过临时写入 Docker Desktop `/etc/hosts` 的正确 Docker Hub IPv4 完成推送，推送后已清理临时映射。
- GitHub 发布：提交 `81851c9 release v0.1.3` 已推送到 `main`，tag `v0.1.3` 已推送到 GitHub，并创建正式 Release：`https://github.com/jojhaa/sublinkx-rs/releases/tag/v0.1.3`。
- Release notes：GitHub Release 说明已写入 `v0.1.3` 更新日志、上游订阅管理变化、Mihomo/Clash 模板优化、导出兼容性修复、Docker 镜像标签与 digest、以及本轮验证命令。

## 2026-07-17

- 功能增强：上游订阅新增定时同步配置，支持为每个上游订阅单独启用同步并设置同步间隔；后端启动后会后台扫描到期订阅并自动执行同步。
- 覆盖同步：同步会按上游当前内容覆盖同来源节点，保留已有节点启用状态，优先更新同指纹节点；当上游只是参数变化导致指纹变化但节点名称不变时，会在同名节点唯一的前提下原地覆盖，避免丢失节点 ID 和已有测速记录；托管上游同步串行执行，避免后台同步和手动同步撞车；如果本次解析出现失败，会跳过停用旧节点以降低误停风险。
- 同步停用：上游同步不再删除上游已移除节点，而是设置 `enabled=0` 和 `upstream_missing=1`，让节点保留在本地但不再参与使用；如果后续上游重新出现该节点，同步会自动清除 `upstream_missing` 并恢复启用。
- 前端管理：上游订阅编辑弹窗新增“启用定时同步”和“同步间隔”字段，列表卡片展示同步状态，最近导入统计扩展为导入、更新、停用、跳过、失败五项。
- 数据迁移：新增 `0015_upstream_subscription_sync.sql`，为上游订阅表补充同步开关、同步间隔、节点 `upstream_missing` 标记和最近一次更新/停用统计；MySQL 启动升级逻辑同步补齐字段与索引。
- 发布准备：后端、前端和锁文件版本号更新到 `0.1.4`；`CHANGELOG.md` 新增 `v0.1.4 - 2026-07-17`，整理上游定时同步、覆盖更新和上游移除节点自动停用说明。
- Docker 发布：使用 `DOCKER_REGISTRY=docker.m.daocloud.io` 构建并推送后端、前端 `latest`、`0.1.4`、`v0.1.4` 标签；后端远端 digest 为 `sha256:59344b70b6ff7f11ba69d9f0a64ed87809d4de71668e4a3e5bfe5284535bce19`，前端远端 digest 为 `sha256:93dd1f832623e6789f433a4d787e062dd08f674150740b24fe524876adeb9642`。
- 发布验证：`docker buildx imagetools inspect docker.io/jojhaa/sublinkx-rs-backend:0.1.4` 和 `docker buildx imagetools inspect docker.io/jojhaa/sublinkx-rs-frontend:0.1.4` 均可读取远端镜像 digest。

## 2026-07-18

- 缺陷修复：订阅管理的节点选择器新增“节点状态筛选”，默认只显示启用节点，可切换查看暂停节点或全部节点，避免暂停节点和上游缺失自动停用节点默认混在可选列表里。
- 缺陷修复：节点管理列表新增“节点状态筛选”，默认只显示启用节点，可切换查看暂停节点或全部节点，方便定位和恢复上游同步自动停用的节点。
- 功能增强：模板管理新增多选批量删除，自定义模板可勾选后批量删除；系统内置模板显示“系统内置”标记，并在前端和后端同时禁止删除。
- 发布准备：项目版本号更新到 `0.1.5`，`CHANGELOG.md` 新增 `v0.1.5 - 2026-07-18`，整理本轮管理端筛选和模板内置保护更新说明。
- Docker 发布：使用 `DOCKER_REGISTRY=docker.m.daocloud.io` 构建并推送后端、前端 `latest`、`0.1.5`、`v0.1.5` 标签；后端远端 digest 为 `sha256:ce2a2040cdd26c7b1683046887d7987d1f08ed9026e604996e82464936b634bc`，前端远端 digest 为 `sha256:d2cce8c3631c818e9f27dcc25bdc476ab4c6065314614ee19bebefa19b8bb89f`。
- 发布验证：`docker buildx imagetools inspect docker.io/jojhaa/sublinkx-rs-backend:0.1.5` 和 `docker buildx imagetools inspect docker.io/jojhaa/sublinkx-rs-frontend:0.1.5` 均可读取远端镜像 digest。

## 2026-07-30

- 前端优化：模板管理页取消宽表格布局，改为左侧模板类型筛选和右侧模板卡片网格；模板内容预览固定在卡片内折叠，操作区不再被长内容挤到页面边缘。
- 发布准备：后端、前端和锁文件版本号更新到 `0.1.6`；`CHANGELOG.md` 新增 `v0.1.6 - 2026-07-30`，整理模板管理卡片式布局和响应式优化说明。
- 验证：`cargo fmt --manifest-path backend\Cargo.toml`、`cargo test --manifest-path backend\Cargo.toml`、`npm --prefix frontend run build`、`git diff --check` 均通过。
- Docker 发布：使用 `DOCKER_REGISTRY=docker.m.daocloud.io` 构建并推送后端、前端 `latest`、`0.1.6`、`v0.1.6` 标签；后端远端 digest 为 `sha256:e5fa0bfda46fc1a7e3bb92f02d4fe3b720a3720fa29c8487d56d4b513d47b841`，前端远端 digest 为 `sha256:90204f9c69c81aa52216523ce95ccfa58aa252b26063c0208d7234e7ad63f905`。
- 发布验证：`docker buildx imagetools inspect docker.io/jojhaa/sublinkx-rs-backend:0.1.6` 和 `docker buildx imagetools inspect docker.io/jojhaa/sublinkx-rs-frontend:0.1.6` 均可读取远端镜像 digest。
- 性能修复：模板管理页新增卡片分页，默认每页只渲染 12 个模板，支持 12/24/48 切换；筛选模板类型时自动回到第一页，“选择当前页”只选择当前页可删除模板，降低 44 个模板同时渲染造成的卡顿。
- 验证：模板分页修复后 `npm --prefix frontend run build` 和 `git diff --check` 均通过。
- Docker 重发：基于模板分页性能修复重新构建并覆盖推送后端、前端 `latest`、`0.1.6`、`v0.1.6` 标签；后端远端 digest 为 `sha256:6f6542beb028e2552f835f764f3c67cee18903706e5acf4fcb4406d80b5253bd`，前端远端 digest 为 `sha256:d40740ada6aac128517815d534f29d19ecd4bccccbdf19ee9f018c705d04b285`。
- 发布验证：`docker buildx imagetools inspect docker.io/jojhaa/sublinkx-rs-backend:0.1.6` 和 `docker buildx imagetools inspect docker.io/jojhaa/sublinkx-rs-frontend:0.1.6` 均可读取新的远端镜像 digest。

## 2026-08-20

- 订阅稳定性修复：节点删除前检查 `subscription_nodes` 引用，已被普通订阅使用的节点会拒绝硬删除，避免外键级联清空订阅节点关系导致订阅链接失效。
- 上游删除保护：删除上游订阅时若选择“同时删除相关节点”，后端会先检查这些上游节点是否仍被普通订阅引用；存在引用时拒绝删除，要求先从订阅移除或选择仅解除上游来源关联。
- 前端交互：节点删除和批量删除确认文案补充订阅引用保护；订阅 token 轮换新增二次确认并明确提示旧链接立即失效。
- 链接稳定性：复制和二维码使用的默认订阅链接改为固定携带订阅默认客户端 `target`，减少因 User-Agent 推断不同导致同一链接在不同客户端表现不一致。
- 按组同步：新增 `0017_subscription_node_groups.sql`，订阅可以同时保存手工节点和自动跟随的节点分组；读取订阅和生成链接时动态合并分组节点，上游同步新增或替换节点后无需重新编辑普通订阅。
- 导出保护：暂停节点和标记为 `upstream_missing` 的节点不再进入订阅导出；上游覆盖同步完成后清空 30 秒公开导出缓存，确保新节点信息立即生效。
- 前端管理：订阅编辑器增加跟随分组面板和一键“跟随当前分组”，列表与详情展示跟随数量；现有固定节点订阅不做自动迁移，只有主动选择分组后才开启跟随。
- 验证：新增 SQLite 迁移与分组节点解析测试；`cargo test --manifest-path backend\Cargo.toml` 49 项通过，`npm --prefix frontend run build` 通过。
- 发布准备：前后端版本更新到 `0.1.7`，新增中文 `v0.1.7` 更新日志，发布范围为订阅数据稳定性修复和按节点分组自动更新功能。
