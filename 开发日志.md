# 开发日志

## 2026-06-22

- 修复远程订阅导入重复判断：节点 fingerprint 去重范围改为当前目标分组，允许不同分组保存相同上游节点。
- 修复上游 Mihomo 模板保存名称：自动保存模板时加入目标分组名，避免只显示 `Upstream Mihomo <digest>` 无法区分来源分组。
- 修复 Mihomo 导出重复代理名：导出前自动将重复 `proxies[].name` 改为 `原名 #2/#3`，并同步展开 `proxy-groups[].proxies` 引用，避免 Mihomo 报 `duplicate name`。
- 追加修复非上游模板导出路径：Mihomo YAML 保存模板前同样执行代理名去重和代理组引用同步，避免保存后的模板内容本身携带重复 `proxies[].name`。
- 验证：`cargo fmt` 通过；`docker build --build-arg DOCKER_REGISTRY=docker.m.daocloud.io --target builder -f backend/Dockerfile -t sublinkx-rs-backend-builder:check .` 通过；`docker run --rm sublinkx-rs-backend-builder:check cargo test mihomo_proxy_name -- --nocapture` 通过；`git diff --check` 通过。
- 追加验证：`docker run --rm sublinkx-rs-backend-builder:check cargo test sanitizes_saved_mihomo_template_duplicate_proxy_names -- --nocapture` 通过。
- 编译：使用 `docker build --build-arg DOCKER_REGISTRY=docker.m.daocloud.io` 完成生产镜像编译；后端镜像 `jojhaa/sublinkx-rs-backend:latest`，镜像 ID `987b70fc4352`，大小 `150MB`；前端镜像 `jojhaa/sublinkx-rs-frontend:latest`，镜像 ID `c8457659f56b`，大小 `93.2MB`。
- 发布：`docker push docker.io/jojhaa/sublinkx-rs-backend:latest` 成功，远端 digest `sha256:987b70fc4352d403d54ae4488f57565290b228c1e9ae18a82ccb285960dc2f76`；`docker push docker.io/jojhaa/sublinkx-rs-frontend:latest` 成功，远端 digest `sha256:c8457659f56bbcf1e8e3a032252a5f57b33b5a7573d3be709ef13d2b5a9abe06`。
- 说明：Docker CLI 不支持直接删除 Docker Hub 远端 tag，本次采用覆盖推送更新 `latest`，避免删除后再上传期间远端镜像不可拉取。
- 配置修复：本地 `.env` 将 `JWT_SECRET` 替换为 32 字符以上随机值，将首次引导密码替换为非默认随机值，并补齐 `MYSQL_PASSWORD`/`MYSQL_ROOT_PASSWORD` 以通过 compose 插值校验。
- 配置验证：`docker compose config --quiet` 通过。
- 功能修复：Mihomo/Clash 导出会把模板中的 `include-all-proxies: true` 展开为显式节点列表，并在模板已有 `AUTO` 组时不再追加重复 `AUTO` 组，避免 178 个已选节点在客户端只显示少量节点。
- 部署修复：前端 Nginx 增加 Docker DNS resolver 和运行时 upstream 变量，避免 `backend` 服务未就绪时启动失败。
- 验证：`docker run --rm -v ${PWD}/frontend/nginx.conf:/etc/nginx/conf.d/default.conf:ro docker.m.daocloud.io/library/nginx:1.29-alpine nginx -t` 通过；`docker run --rm sublinkx-rs-backend-builder:check cargo test include_all_proxy_groups -- --nocapture` 通过。
- 说明：本机 `cargo check` 受限于缺少 MSVC `link.exe`，未能进入项目代码编译阶段；已用 Docker/Linux 构建路径完成验证。
- 编译发布：重新构建并推送 Docker Hub `latest` 镜像；后端镜像 ID `3ca280fae582`，大小 `150MB`，远端 digest `sha256:3ca280fae582589881c102820fc2e0b4c91247618d5438f397f0cb2df38b44d7`；前端镜像 ID `c16104f11122`，大小 `93.2MB`，远端 digest `sha256:c16104f11122516aef809d01abd0074afcbcfe654a5a271dab62da5912aed89c`。
- 修复 Mihomo 普通导出器测速失败：无上游模板时现在生成 `PROXY` select 组和 `AUTO` url-test 组，`AUTO` 不再错误输出为 `select`；已有模板规则不再被强行追加 `MATCH,AUTO`。
- 调整默认测速 URL：Mihomo 导出器、内置模板、后端默认设置和前端占位值统一使用 `https://cp.cloudflare.com/generate_204`；新增迁移仅将仍为旧默认 `https://www.gstatic.com/generate_204` 的设置和内置 Clash/Mihomo 模板替换为新默认，不覆盖用户自定义 URL。
- 验证：`cargo fmt` 通过；`docker build --build-arg DOCKER_REGISTRY=docker.m.daocloud.io --target builder -f backend/Dockerfile -t sublinkx-rs-backend-builder:check .` 通过；`docker run --rm sublinkx-rs-backend-builder:check cargo test mihomo -- --nocapture` 通过；`backend\mihomo\mihomo.exe -t` 对新 `PROXY + AUTO url-test` 配置校验通过；`npm run build` 通过；`git diff --check` 通过。

## 2026-06-23

- 定位 Mihomo 导出节点全部不可用原因：用户提供的 `测试.yaml` 触发 `GEOIP,CN` 和 DNS `fallback-filter` 的 MMDB 初始化，当前环境无法下载 `geoip.metadb` 时 Mihomo 整份配置启动失败；原始 `iNetS` 可用是因为使用 `RULE-SET`/`no-resolve` 规则，没有强依赖 GeoIP 数据库。
- 修复内置 Clash/Mihomo 路由模板：移除默认模板中的 `dns.fallback`、`fallback-filter` 和 `GEOIP,CN` 规则；更新模板文档；SQLite 迁移和 MySQL 启动升级会清理旧内置模板中的同类默认片段。
- 验证：`backend\mihomo\mihomo.exe -t` 对移除 fallback/GeoIP 后的用户配置校验通过；`docker run --rm sublinkx-rs-backend-builder:check cargo test built_in_clash_template_avoids_geoip_database_dependency -- --nocapture` 通过。
- 补全默认导出规则：Clash 内置模板扩展为 ACL4SSR 风格常用分流，Mihomo 内置模板扩展为接近 iNetS 的 MetaCubeX `.mrs` rule-provider 分流，覆盖 AI、GitHub、YouTube、Google、Telegram、Netflix、Bilibili、Spotify、Steam、PayPal、Microsoft、Apple、国内直连和兜底策略。
- 修复无模板 Clash 导出：默认模板提取逻辑增加中文标题匹配，避免实际取到“最小分流模板”；文档完整模板补齐 `MATCH,漏网之鱼`，并统一使用 `https://cp.cloudflare.com/generate_204`。
- 兼容旧库：后端启动时会仅升级仍停留在旧极简内容、且没有 `rule-providers` 的内置 Clash/Mihomo 模板；用户自定义模板不覆盖。
- 验证：新的 Mihomo 内置模板加 dummy 节点后通过 `backend\mihomo\mihomo.exe -t`；文档完整 Clash 模板加 dummy 节点后通过 `backend\mihomo\mihomo.exe -t`；`cargo fmt`、Docker builder 目标单测和 `git diff --check` 通过。
- 编译发布：使用 `docker build --build-arg DOCKER_REGISTRY=docker.m.daocloud.io` 完成生产镜像编译并推送 Docker Hub `latest`；后端镜像 `jojhaa/sublinkx-rs-backend:latest`，镜像 ID `31f52aa551e9`，大小 `150MB`，远端 digest `sha256:31f52aa551e9f681a1b5fecbc43679c721c33ed92b203ebd19c7770a34de185a`；前端镜像 `jojhaa/sublinkx-rs-frontend:latest`，镜像 ID `3753cdb6382d`，大小 `93.2MB`，远端 digest `sha256:3753cdb6382d41a2c6ee46a07f2df25d25f90a883600761e950e266b735e4daf`。
- 部署修复：分析前端 Nginx 日志确认 `backend could not be resolved` 是 Docker DNS 中没有健康后端服务导致；后端镜像加入 `curl`，Compose 为后端增加 `/healthz` 健康检查，并让前端 `depends_on` 等待 `service_healthy`，减少后端启动失败时前端直接暴露 502 的情况。
- 文档：中英文 Docker 部署文档补充 `backend could not be resolved` 排查方式，优先执行 `docker compose ps` 和 `docker compose logs --tail=200 backend`。
- 编译发布：重新构建并推送后端镜像 `jojhaa/sublinkx-rs-backend:latest`；镜像 ID `912e2d96e9e2`，大小 `158MB`，远端 digest `sha256:912e2d96e9e251e2f41e11d5c99a452061da056d9262c3283489dfd1fd6acd68`；`docker compose config --quiet` 和 `git diff --check` 通过。
- 部署修复：恢复历史迁移 `0006_latency_real_link_settings.sql` 原始内容，解决已执行过第 6 号迁移的线上 SQLite 数据库在新镜像启动时报 `Migrate(VersionMismatch(6))`；测速 URL 迁移继续由 `0012_latency_test_url_default.sql` 负责，不修改历史迁移校验。
- 编译发布：重新构建并推送后端镜像 `jojhaa/sublinkx-rs-backend:latest`；镜像 ID `84e71d866a83`，大小 `158MB`，远端 digest `sha256:84e71d866a8351b25a0fac780185532afa8eeead8adf128bc9f666a7838d2cc8`；`git diff --check` 通过。
- 发布补充：为绕过部署端 `latest` 缓存问题，额外推送后端不可变标签 `docker.io/jojhaa/sublinkx-rs-backend:20260623-migrate-fix`，远端 digest `sha256:84e71d866a8351b25a0fac780185532afa8eeead8adf128bc9f666a7838d2cc8`。
