# Changelog

## v0.2.28 - 2026-09-11

- 修正 Clash/Mihomo“仅节点”切换策略不生效：移除网站规则与远程规则集，但保留规则模式和指向主策略组的唯一 MATCH 兜底；“节点＋规则”完整分流不变。兼容“节点选择”“代理选择”与自定义组名，不改订阅链接或数据库。

## v0.2.27 - 2026-09-11

- 订阅新增/编辑支持选择“仅节点”或“节点＋规则”。仅节点仍保留国家分组、负载均衡、故障转移等策略组，不携带 DNS、网站分流或规则集；历史订阅与新建订阅均默认仅节点，旧链接和 Token 不变。
- 内容偏好随原订阅链接、二维码和门户生效，保留 Token，补齐数据库升级与缓存隔离。

## v0.2.26 - 2026-09-09

- 修正 Clash 文本规则集格式，补充国内 DNS、国内域名直连与 Fake-IP 排除；Mihomo 直连解析不再跟随国外策略，国内 IP 规则允许解析后匹配。
- 导出时兼容旧系统模板，不强制修改自定义或上游透传模板，保留订阅链接与节点数据。
- 独立门户支持唤起客户端导入；无法确认协议支持的客户端提供手动操作说明，不假装检测安装状态。
- 生产发布支持校验后加载本地构建镜像，保留旧镜像回滚。配置加载检查不代替真实客户端访问验收。

## v0.2.25 - 2026-09-07

- 增加独立 PostgreSQL 测试部署配置，数据库仅在项目 Docker 网络内开放，支持数据持久化与日志轮转。
- 增加本地镜像 SSH 传输、SHA-256 校验和测试环境登录验收脚本。
- 测试部署随机生成初始凭据，敏感文件和镜像归档排除在 Git 与 Docker 构建上下文之外。

## v0.2.24 - 2026-09-02

### Quantumult X 节点语法与 Shadowrocket 策略链修复

- Quantumult X 节点端点统一使用客户端支持的 `host:port` 语法，IPv6 自动加方括号；VLESS Vision 改用 `vless-flow`，节点标签统一放在参数末尾。
- Quantumult X 完整配置会过滤当前客户端尚不支持的 Hysteria2 节点，并通过响应头报告过滤数量；Mihomo、Shadowrocket 等其他导出目标不受影响。
- Shadowrocket 完整配置用本次成功导出的全部节点重建“手动选择”和“自动选择”，修复空自动组导致配置模式回退使用首页节点的问题。
- Shadowrocket 最终规则固定进入“代理选择”，再由“手动选择”“自动选择”、风险策略组或直连决定出口；普通订阅链接、节点状态和订阅 Token 保持不变。

## v0.2.23 - 2026-09-02

### iOS 完整配置入口与门户自适应修复

- Quantumult X 内置完整配置改用客户端官方样例兼容的 HTTP 检测地址，修复 `malformed server check URL` 导入错误；用户自定义模板保持不变。
- 新增 `/s/{token}/profile/{target}` 完整配置专用路径，仅允许 Quantumult X 和 Shadowrocket 完整配置，避免 Shadowrocket Scheme 丢失查询参数后回退成节点订阅。
- 门户与订阅管理后台统一使用无查询参数的完整配置路径；原 `/s/{token}` 和 `?target=` 链接继续兼容，订阅 Token、节点关系和数据不变。
- 独立订阅门户完成桌面、平板和手机响应式布局，客户端卡片分别使用 4/2/1 列并优化窄屏操作与二维码弹窗。

## v0.2.22 - 2026-09-02

### Quantumult X 与 Shadowrocket 导入修复

- Quantumult X 内置完整配置不再读取数据库中可能过期的系统模板正文，固定输出单一 `[server_local]` 和规范的 `server_check_url=https://cp.cloudflare.com/generate_204`，避免旧 `[server_remote]` 残留造成配置导入失败。
- 内置 Quantumult X 基础配置移除非必要的第三方地理检查脚本和图标 URL，降低客户端解析及首次导入时的外部依赖。
- Shadowrocket 完整配置安装改为原生 `shadowrocket://config/add/{原始 HTTPS 配置地址}` 链接，并使用直接 `<a href>` 用户手势跳转；不再对整个配置地址执行百分号编码。
- Shadowrocket 保留“复制配置地址”回退，客户端未跳转时可在配置页面手动导入；普通 `/s/{token}` 订阅链接和 Token 不变。

## v0.2.21 - 2026-09-02

### 独立订阅门户

- 每个普通订阅可独立启用外部订阅门户，通过单独随机页面标识和访问码进入；页面展示订阅状态、到期时间、客户端链接和二维码。
- 访问码支持任意字符，服务端仅保存 Argon2 哈希；公开验证接口按真实客户端 IP 限流，页面标识和订阅 Token 均从请求日志中脱敏。
- 门户链接在每次验证成功时根据当前订阅 Token 生成，会跟随后台节点分组、订阅状态、到期时间和 Token 轮换，不复制或冻结节点数据。
- 新增 SQLite、MySQL、PostgreSQL 兼容迁移；历史订阅默认关闭门户，不改变现有订阅链接、节点关系和加密数据。

## v0.2.20 - 2026-09-02

### Quantumult X 与 Shadowrocket 风险策略组

- Quantumult X 完整配置改为把本地节点写入标准 `[server_local]`，补齐 AnyTLS、主策略、国内直连和最终代理规则，并修正 WebSocket TLS/Reality 字段。
- Quantumult X 现在生成全局 `0欺诈`、`低欺诈`、`手动选择住宅网络`，以及按中文国家名称生成的零欺诈/低欺诈故障转移策略。
- 新增独立 `shadowrocket-profile` 完整配置目标，输出节点、自动选择、风险故障转移、全局住宅手选和基础规则；原 `shadowrocket` URI Bundle 保持兼容不变。
- 两个目标与 Mihomo 共用同一套风险资格判定，并只引用实际成功渲染的节点；刷新配置时会移除旧保留组，避免重复或残留。

## v0.2.19 - 2026-09-02

### 全局住宅网络分组修复

- Mihomo `手动选择住宅网络` 明确作为跨国家的全局 `select` 大分组，不依赖节点是否具有国家代码。
- 住宅判定使用 IP 情报服务已完成归一的 `usage_type=residential` 结果，不再因该字段存在来源分歧而将全部候选误删。
- 代理、托管、滥用、中继、Tor、VPN、僵尸网络和威胁等级仍保持严格失败关闭；欺诈分与 ISP 风险分仍必须均低于 10。

## v0.2.18 - 2026-09-02

### 严格欺诈与住宅网络分组

- `0欺诈` 现在同时要求 Scamalytics 欺诈分和 ISP 风险分均为 0，并明确排除滥用、中继、Tor 与 VPN 网络。
- `低欺诈` 要求欺诈分 1–20、ISP 风险分 0–20，并应用相同的网络安全特征约束。
- 新增 Mihomo `手动选择住宅网络` 分组，只收录明确识别为住宅、非代理、非托管、无僵尸网络和有效威胁命中，且欺诈分与 ISP 风险分均低于 10 的节点。
- 网络特征、分数、缓存任一过期、缺失、冲突或出口 IP 不一致时均不进入上述严格分组；出口 IP 变化和上游重同步会自动清除旧特征快照。

## v0.2.17 - 2026-09-02

### Mihomo 国家分组中文化

- 为 ISO 3166-1 国家和地区代码补齐简体中文名称，动态欺诈分组不再显示 `KR`、`TR`、`DE` 等原始代码。
- 韩国、土耳其、德国等国家的零欺诈和低欺诈故障转移组统一使用中文名称；未知代码显示为“未知地区”。
- 仅改变导出 YAML 中的策略组显示名称，不修改节点、订阅、国家代码或风险数据。

## v0.2.16 - 2026-09-02

### IP 风险联动与订阅导出

- 对接 IP 情报批量查询，根据节点当前出口 IP 复用有效缓存，并在出口 IP 变化或缓存过期时重新检测。
- 增加 Scamalytics 欺诈分与 ISP 风险展示，以及零欺诈、低欺诈和对应国家故障转移 Mihomo 动态分组。
- 增加可选风险导出拦截，欺诈分大于 50 的节点不再进入订阅导出，但不改写节点原有启用状态。
- 上游重新同步后使旧出口 IP 风险结果失效，通过 revision 防止异步旧结果覆盖新 IP。
- 风险检测失败或供应商缓存暂时过期时保留最近一次有效结论，避免短暂外部故障造成订阅节点无故抖动。

## v0.2.15 - 2026-09-01

### 节点风险分批量查询

- 新增登录后接口 `POST /api/v1/node-ip-probes/intelligence/risk-scores`，一次最多查询 200 个节点的 Scamalytics 欺诈分和 ISP 风险分。
- 使用节点最近一次成功探测的公网出口 IP；多个节点共用同一出口 IP 时只查询情报服务一次，并将外部请求限制为最多 8 个并发。
- 每个节点独立返回 `complete`、`partial`、`pending` 或 `failed`，区分完整分数、部分分数、缓存待补全、无有效出口 IP及远端失败。
- 情报查询只读取 `ip-intelligence-rs` 的本地缓存；首次缺失或缓存过期时由情报服务异步补全，本次接口不会阻塞等待第三方 API。
- 不启动 Mihomo，不修改节点、订阅或 IP 探测数据，出站只发送去重后的公网 IP，不发送节点协议、认证信息或订阅正文。

## v0.2.14 - 2026-09-01

### Mihomo 内嵌规则

- 使用系统内置 Mihomo Policy Orchestrator 导出时，23 个域名/IP 规则 Provider 改为随订阅 YAML 内嵌，客户端首次加载不再依赖 GitHub Raw 下载规则。
- 国内域名、国内 IP 和代理域名使用 MetaCubeX 官方 `geo-lite` 常用集合，最终配置保持在几百 KB，而不是展开十几万条完整地理数据库规则。
- 内嵌仅作用于精确匹配的系统模板；用户自定义模板和上游 Mihomo 透传模板保持原样。
- 公共订阅响应缓存增加 64 项/64 MiB 双重上限，内嵌规则增大单条响应后仍不会让缓存无界占用内存。
- 规则快照固定到可追溯的上游提交，并新增更新脚本、SHA-256 来源清单、第三方 NOTICE 和 GPL-3.0 许可证副本。
- 未修改数据库、订阅 Token、节点、模板正文或现有加密数据。

## v0.2.13 - 2026-09-01

本版本精简 Clash/Mihomo 系统模板的策略组展示，保留完整分流能力，减少客户端策略页面的重复卡片。

### 策略组精简

- Clash 与 Mihomo 系统模板只保留节点选择、手动选择、自动选择、故障转移和负载均衡五个基础策略组；国家负载与国家故障转移仍按节点国家动态生成。
- DNS、规则 Provider 和完整规则列表全部保留；网站与服务规则统一交给主节点选择，国内规则直连，广告与下载拦截规则直接拒绝。
- Clash 的“手动切换”统一为“手动选择”，避免导出器再补一个同用途策略组。
- 兼容数据库中已经保存的旧系统模板：导出时自动压缩旧分组并改写规则目标，不修改模板记录、订阅、节点或 Token。
- 自定义模板和上游 Mihomo 透传模板保持原样，不参与系统模板精简。

## v0.2.11 - 2026-09-01

本版本修复普通订阅未手动选择模板时没有按导出目标加载系统模板的问题。

### 自动系统模板

- 普通订阅的 `template_id` 为空时，按最终导出目标自动选择同类型、最新的系统内置模板；User-Agent 自动协商目标时同样生效。
- 手动选择且类型匹配的系统、自定义或上游模板继续优先；手动模板与目标不匹配时回退到目标系统模板。
- Mihomo 默认使用新版 `Built-in Mihomo Policy Orchestrator`，自动获得 DNS、规则提供者和完整分流规则，不再只输出单条 `MATCH` 兜底规则。
- Clash、Mellow、Surge、sing-box、Quantumult X、Quantumult、Loon、Surfboard、SIP008 和 SSD 同步接入各自系统模板。
- Xray、SS、SSR、Trojan 和 Mixed 等 URI 列表目标保持纯 URI 正文，说明性系统模板不会混入客户端订阅。

## v0.2.10 - 2026-08-31

本版本修复模板管理页在大型 Mihomo 模板较多时卡死的问题。

### 模板管理性能

- 模板分页列表改为只返回最多 320 字符的正文预览，避免 CSS 虽然截断显示、浏览器仍需接收和排版数 MB 完整 YAML。
- 点击编辑时通过单条详情接口按需读取完整模板，确保保存时不会用预览内容覆盖原始配置。
- 模板列表使用浅层响应式数据，继续保留服务端分页、类型筛选、批量选择和上游透传模板识别。

## v0.2.9 - 2026-08-31

本版本修复多个 Compose 项目共享 Docker 网络时，通用 `backend` 服务名发生 DNS 冲突而导致接口间歇性失败的问题。

### 生产网络

- 后端在项目网络中增加唯一别名 `sublinkx-backend`，前端 Nginx 仅通过该别名连接本项目后端。
- 避免同一共享网段中的其他项目也使用 `backend` 服务名时，Docker DNS 返回错误容器地址并引发偶发连接失败或页面卡顿。

## v0.2.8 - 2026-08-31

本版本补齐生产验收中发现的订阅 Token 日志边界，并将历史上游来源发现改为一次性兼容迁移。

### 日志与上游管理

- `/s/{token}` 继续关闭访问日志，并在该路由内仅保留 Nginx `crit` 及以上错误，避免后端短暂重启时连接错误把完整订阅 Token 写入容器日志；其他路由和全局错误日志不受影响。
- 历史节点来源回填仅对从未建立上游管理记录的旧数据库执行一次；已有上游记录的数据库写入完成标记后跳过发现，避免已删除或已编辑的旧上游在服务重启后重新出现。

## v0.2.7 - 2026-08-31

本版本收敛 Mihomo 上游透传模板：每个上游来源只保留最新模板，避免重复同步后模板列表持续出现 `#2/#3...` 历史版本。

### 透传模板

- 上游 Mihomo YAML 导入或定时同步时，按上游 URL 摘要复用并更新该来源的最新透传模板，不再为相同来源持续新增模板记录。
- 服务启动时自动整理符合既有命名规则的历史透传模板，按模板 ID 保留最新版本，并删除旧自定义版本。
- 删除旧模板前在同一数据库事务内迁移普通订阅和上游订阅的模板绑定，避免订阅链接因模板清理失效。
- 模板内容或绑定更新后立即清除公开订阅导出缓存，客户端下一次请求直接使用最新配置。
- 系统内置模板、其他上游来源和无法识别来源摘要的手工模板不会被自动删除。

## v0.2.6 - 2026-08-31

本版本加入 PostgreSQL 运行与离线数据迁移能力，并把 Mihomo 内置策略组统一为更直观的中文名称。数据库切换不会自动发生，必须显式预检、备份和确认后执行。

### PostgreSQL

- 新增 PostgreSQL 14+ 数据库支持，覆盖初始化建表、索引、默认设置、管理员与模板初始化，以及节点、订阅、上游订阅、分页和 IP 探测等现有数据访问路径。
- Docker Compose 增加可选 `postgres` profile，并提供 PostgreSQL 16 Alpine 配置示例和隔离容器冒烟脚本。
- 新增离线 `migrate-database check/run` 工具，支持在 SQLite、PostgreSQL 与 MySQL/MariaDB 之间迁移完整业务数据，保留主键、密码哈希、订阅 Token 和所有关联关系。
- 迁移默认要求空目标库和显式备份确认，源库只读、目标单事务写入并逐表核对数量；失败整体回滚，PostgreSQL 自增序列会在成功前校正。
- 增加 Windows/Linux 迁移脚本，以及 SQLite 往返、真实 PostgreSQL、真实 MySQL 往返和失败回滚测试；修改连接仍不会自动搬库。
- Docker Compose 为前端、后端及可选数据库统一启用 `json-file` 日志轮转，单文件上限 10 MB，保留 3 份，避免生产日志长期无界增长。

### Mihomo 策略组

- 动态配置与内置策略模板统一使用“代理选择”“手动选择”“自动选择”“故障转移”“负载均衡”等中文策略组名称。
- 导出阶段兼容已保存的旧英文系统模板，同步转换组成员、规则目标、DNS 分流标签和带 `no-resolve` 的规则引用；上游自定义组名保持不变。
- `DIRECT`、`REJECT`、Mihomo 类型值等配置语法关键字不做翻译，避免影响客户端加载。

## v0.2.5 - 2026-08-30

本版本汇总 `v0.2.0` 之后已完成并投入生产的订阅兼容、真实链路测试、出口 IP 探测、国家情报和 Mihomo 策略编排能力，同时补齐后台运行设置与大列表读取优化。

### 订阅兼容与上游同步

- 公开订阅支持按客户端 User-Agent 自适应返回 Mihomo YAML、sing-box JSON 或 v2rayN/Xray URI Bundle，指定 `target` 的旧链接继续保持固定格式。
- 上游导入兼容 Mihomo/Clash `proxies`、Provider `payload`、SIP008 Shadowsocks JSON、UTF-8 BOM 和双层 Base64，并优化异常响应诊断。
- v2rayN Bundle 增加 AnyTLS、TUIC 和 WireGuard；Mihomo YAML 导入继续覆盖 Shadowsocks、VMess、VLESS、Trojan、Hysteria2、TUIC、WireGuard 与 AnyTLS。
- 公开订阅改为附件下载并增加禁止索引、跟踪和归档响应头；渲染缓存与单 IP/全局限流支持在系统设置中调整。
- 订阅与模板候选使用轻量响应，减少大模板正文、节点协议详情和原始链接造成的页面等待与响应体积。

### 链路测试与出口 IP

- 新增独立“链路测试”页面，支持按分组、状态、搜索或手动选择节点，并使用单个 Mihomo 会话执行 1/3/5 轮实时测试。
- 实时展示成功、失败、错误原因、最低/平均/最高延迟、抖动和成功率；默认测试目标可选择同步更新节点最后延迟。
- 新增“IP 探测”页面，批量获取节点真实公网出口 IPv4/IPv6；支持手动、定时和上游导入成功后异步探测。
- 链路测试、手动测速、后台测速和 IP 探测共用任务锁，支持停止冲突任务，并确保 Mihomo 子进程及临时配置得到清理。

### 国家情报与 Mihomo 策略

- 对接独立 `ip-intelligence-rs` 服务，按出口 IP 回填国家信息；国家名称在管理端显示为中文，数据库与 API 保留稳定国家代码。
- Mihomo 导出可按可信国家信息生成区域负载与故障转移组，并支持在系统设置中调整最少节点门槛。
- 系统编排模板增加手动选择、最低延迟、故障转移、一致性哈希负载、AI/流媒体/Google/Telegram/游戏平台分流、BT 阻断和国内外 DNS 策略。
- 不再把数据库节点分组直接渲染成额外策略卡片，减少客户端策略页面冗余；旧客户端需更新订阅后生效。

### 设置与运行安全

- 系统设置新增链路测试默认目标、默认轮数、延迟回写、IP 定时探测、导入后探测、国家自动检测、公开订阅缓存与限流，以及国家策略组门槛。
- JWT、数据库连接、Cookie、可信代理、登录封禁、SSRF 固定目标、导入数量和下载体积上限继续只允许通过服务端安全配置控制。
- Docker 发布脚本改为 Windows 本地构建 `linux/amd64` 镜像后推送，并检查所有外部命令退出码；固定镜像标签简化为项目组件名加版本号。

### 数据升级

- 新增迁移 `0018_node_ip_probes.sql` 和 `0019_node_ip_intelligence.sql`，仅增加出口 IP 与情报同步记录结构，不轮换 JWT、订阅 Token 或现有加密数据。
- 升级前仍建议备份数据库；本次生产升级已验证 SQLite 完整性以及节点、订阅、模板和上游数量保持不变。

## v0.2.0 - 2026-08-27

本版本集中优化管理后台的数据读取链路，降低 MySQL 连接占用、列表首屏响应体积和大数据量页面渲染压力。升级不包含数据库迁移，不会修改现有节点、订阅、Token 或加密数据。

### 查询性能

- 节点、普通订阅、模板和上游订阅列表改为服务端分页，并支持在数据库查询阶段应用分组、状态和模板类型筛选。
- 普通订阅列表关联节点与节点分组改为批量查询，移除逐订阅、逐节点读取造成的 N+1 查询。
- 普通订阅列表使用轻量摘要响应，不再为每条订阅重复返回完整节点对象；编辑器继续按需加载节点详情。
- 系统设置由 7 次逐键读取合并为 1 次批量读取，上游历史来源回填从普通 GET 热路径移到服务启动阶段单次执行。

### 管理后台

- 总览页新增专用统计接口，一次查询返回节点、订阅、模板和上游订阅数量，不再下载完整节点与订阅列表只为计数。
- 节点、普通订阅、模板和上游订阅页面接入真实服务端页码，筛选和每页数量切换不再在浏览器中处理全量数据。
- 数据新增、更新、移动和删除后仅刷新相关列表，减少重复请求并保持分页总数一致。

## v0.1.9 - 2026-08-27

本版本修复 Trojan 节点从 Mihomo YAML 导入后无法连接、延迟检测直接失败的问题，重点恢复密码、SNI、传输层和 TLS 扩展配置的完整往返。

### Trojan 兼容性

- 修复 Trojan URI 百分号编码密码未解码的问题，包含 `@`、`:`、`%` 等字符的密码可正确往返。
- Mihomo Trojan 导出改用协议要求的 `sni` 字段，不再错误输出 VMess/VLESS 使用的 `servername`。
- 保留 Trojan Reality、完整 WebSocket/gRPC、证书指纹、ECH、ShadowTLS、ResTLS、JLS、Trojan-Go Shadowsocks、SMUX 及常用网络选项，避免上游转换后握手参数丢失。
- 延迟测试继续复用统一 Mihomo 渲染器，因此重新同步后的 Trojan 节点会使用修复后的配置进行检测。

## v0.1.8 - 2026-08-27

本版本扩展 Mihomo YAML 上游订阅导入能力，减少多协议订阅中节点被静默忽略的情况，并加强复杂协议配置的字段保真与失败保护。

### 上游协议导入

- Mihomo YAML 上游新增 VMess、TUIC、WireGuard 和 AnyTLS 导入，连同既有 Shadowsocks、VLESS、Trojan、Hysteria2，共覆盖 8 类当前具有完整导出链路的协议。
- TUIC 同时支持 v4 token 与 v5 UUID/password，并保留 TLS、拥塞控制、UDP 中继等常用参数。
- AnyTLS 保留 SNI、ALPN、客户端指纹、空闲会话参数、证书校验以及 ShadowTLS、ResTLS、JLS 扩展配置。
- WireGuard 支持简化配置与单 peer 完整配置，保留双栈地址、密钥、Allowed IPs、reserved、keepalive、DNS 和 MTU。

### 导入安全

- 未支持协议或字段不完整的节点会进入导入失败明细，不再被静默跳过。
- WireGuard 多 peer 配置会明确失败，避免只导入第一个 peer 后产生行为不一致的节点。
- 上游同步出现解析失败时保留已有节点，不会因为一次异常订阅响应而自动停用仍可用的历史节点。

## v0.1.7 - 2026-08-20

本版本聚焦普通订阅链接的数据稳定性，并让订阅链接能够按节点分组自动跟随上游同步结果。

### 按分组自动更新

- 普通订阅新增“自动跟随节点分组”，支持同时跟随一个或多个上游节点分组。
- 上游同步新增节点、原地更新节点参数后，已跟随该分组的订阅链接会自动使用最新节点，无需重新编辑订阅。
- 上游已移除并标记为 `upstream_missing` 的节点，以及手动暂停的节点，不再进入订阅导出。
- 上游覆盖同步完成后立即清理公开订阅导出缓存，避免短时间继续返回同步前的配置。
- 新增迁移 `0017_subscription_node_groups.sql`；现有固定节点订阅不会被自动转换，需在订阅编辑器中主动选择要跟随的节点分组。

### 数据与链接保护

- 删除节点前检查普通订阅的固定节点引用和分组跟随引用，正在使用的节点会拒绝硬删除。
- 删除上游订阅并选择“同时删除相关节点”时，如果这些节点仍被普通订阅使用，后端会阻止删除，避免订阅链接变为空配置。
- 订阅 Token 轮换增加二次确认，明确提示旧订阅链接会立即失效。
- 默认复制链接和二维码固定携带订阅默认客户端 `target`，减少不同客户端 User-Agent 推断不一致造成的导出差异。

### 管理界面

- 订阅编辑器新增“自动跟随节点分组”选择区，并支持在节点筛选器中一键跟随当前分组。
- 由跟随分组管理的节点会在编辑器中明确锁定，避免误以为单独取消节点后仍会保持排除。
- 订阅列表和详情新增跟随分组数量展示，便于区分固定节点订阅和自动更新订阅。

### 依赖安全

- 更新前端依赖锁中的 Axios、FormData、NanoID、PostCSS、Vite 和 Rolldown 等兼容版本，修复构建审计发现的高危安全公告。
- 更新后 `npm audit --omit=dev --audit-level=high` 返回 0 个漏洞，前端生产构建通过。

## v0.1.6 - 2026-07-30

本版本聚焦模板管理页的布局重构，解决模板内容过长导致页面横向拥挤的问题。

### 模板管理

- 模板管理页从宽表格重构为卡片式布局，右侧模板列表改为响应式卡片网格。
- 新增左侧模板类型筛选面板，支持按模板类型快速收敛列表并显示各类型数量。
- 模板内容预览限制在卡片内部折叠展示，避免长 YAML/规则内容撑宽页面。
- 自定义模板仍支持多选批量删除，系统内置模板继续禁用选择和删除入口。
- 移动端布局改为单列卡片和横向类型筛选，减少窄屏下的横向滚动。
- 模板卡片列表新增分页，默认每页只渲染 12 个模板，支持 12/24/48 切换，避免 44 个模板同时渲染造成卡顿。

## v0.1.5 - 2026-07-18

本版本聚焦管理端筛选体验和系统内置模板保护。

### 节点与订阅管理

- 订阅编辑弹窗的节点选择器新增“节点状态筛选”，默认只显示启用节点，可切换查看暂停节点或全部节点。
- 节点管理列表新增“节点状态筛选”，默认只显示启用节点，方便排查上游同步自动停用的节点。
- 切换节点状态筛选时会重置分页和当前勾选，降低批量操作误选隐藏节点的风险。

### 模板管理

- 模板管理列表新增多选批量删除，自定义模板可勾选后批量删除。
- 系统内置模板新增 `is_builtin` 持久化标记，前端显示“系统内置”徽标并禁用删除入口。
- 后端删除接口同步拒绝删除系统内置模板，即使绕过前端直接调用 API 也不会误删默认模板。
- 新增迁移 `0016_template_builtin_flag.sql`，为已存在的内置模板补充保护标记，不修改历史迁移。

## v0.1.4 - 2026-07-17

本版本聚焦上游订阅的自动同步、覆盖更新和线上数据安全。

### 上游订阅同步

- 上游订阅新增定时同步能力，可为每个上游单独启用同步并设置同步间隔。
- 后端启动后会后台扫描到期上游订阅，自动拉取并按上游当前内容同步节点。
- 托管上游同步串行执行，避免后台同步和手动“立即导入”同时覆盖同一批节点。
- 最近同步统计扩展为导入、更新、停用、跳过、失败，并在管理页持久展示。

### 覆盖更新

- 同步优先按同上游 URL + 指纹覆盖已有节点，保留节点 ID、启用状态和测速记录。
- 当上游只是节点参数变化导致指纹变化、但节点名称不变时，会在名称唯一的前提下按同上游同名节点原地覆盖。
- 手动导入节点仍保持原有 add-only 行为；上游订阅管理页的“立即导入”和定时同步使用覆盖同步。

### 数据安全

- 上游已移除的节点不再从本地删除，而是自动设置为停用并标记 `upstream_missing`，避免继续被使用。
- 如果后续上游恢复该节点，自动同步会清除 `upstream_missing` 并重新启用；用户手动禁用的节点不会被同步擅自启用。
- 若本次上游解析出现失败，会跳过旧节点停用，避免脏订阅或临时异常导致线上节点被误停。

## v0.1.3 - 2026-07-09

本版本聚焦上游订阅管理、Mihomo/Clash 模板默认体验和订阅节点导出稳定性。

### 上游订阅管理

- 上游订阅管理页改为专用卡片列表，长订阅链接只展示域名和短摘要，减少页面被长 URL 撑乱的问题。
- 托管上游订阅取消手动选择节点分组，后端按上游订阅名称自动创建或复用节点分组，节点导入按订阅名隔离。
- 修复上游订阅删除后被已导入节点 `source_ref` 自动回填、导致页面看起来无法删除的问题。
- 删除上游订阅时新增“同时删除相关节点”可选行为；默认仅解除来源关联，选择后才删除仍关联该上游链接的节点。

### Mihomo/Clash 模板

- Mihomo/Clash 默认模板的“节点选择”改为手动选择优先，再进入自动测速选择。
- 无上游模板的 Mihomo 导出同步生成 `PROXY`、`MANUAL`、`AUTO` 三组，客户端默认不再落到自动选择。
- 内置模板补充 AI、GitHub、YouTube、Netflix、Telegram、社媒流媒体和游戏平台等常用站点显式规则。
- 新增迁移更新仍停留在内置模板内容的旧库记录，不覆盖用户自定义模板。

### 导出与兼容性

- 上游订阅自动分组后，重复节点检测按当前目标分组执行，不再被其他分组的相同节点误判为重复。
- Mihomo/Clash 导出会展开 `include-all-proxies: true` 为当前订阅的完整节点列表，避免客户端策略组只显示少量节点。
- Mihomo 保存模板和导出路径都会修正重复代理名，并同步更新策略组引用，避免 `duplicate name`。
- 上游 Mihomo YAML 导入补充 Shadowsocks `type: ss` / `shadowsocks` 兼容，并在再导出时保留 `udp`、`plugin` 等字段。

## v0.1.2 - 2026-07-08

本版本聚焦上游订阅链接管理和 Shadowsocks 上游导入兼容性。

### 上游订阅管理

- 新增上游订阅链接管理页面 `/upstreams`，侧边栏增加“上游订阅”入口。
- 新增 `upstream_subscriptions` 持久化表和管理 API，支持保存、编辑、删除、手动重新导入上游订阅链接。
- 上游订阅列表展示目标节点分组、启用状态、最近导入状态、导入/跳过/失败数量、最近导入时间和上游模板信息。
- 节点页旧的“上游订阅导入”成功后会自动写入管理表；管理页也会从已有节点 `source_ref` 反向补齐历史导入过的上游链接。

### Shadowsocks 导入修复

- 修复 Mihomo YAML 上游中的 `type: ss` / `shadowsocks` 节点无法从上游订阅导入的问题。
- Shadowsocks 解析器增强 SIP002 兼容，支持 `userinfo` Base64、尾部 `/`、`udp` query 和 `plugin` query。
- Mihomo 再导出时保留已导入 SS 节点的 `udp` 和 `plugin` 设置，减少导入后再导出的配置丢失。

## v0.1.1 - 2026-06-23

本版本聚焦已上线环境的安全加固、Mihomo/Clash 导出稳定性、延迟测试体验和 Docker 部署可靠性。

### 安全加固

- 生产环境启动时校验 `JWT_SECRET`、默认引导账号和关键安全配置，阻止默认密钥或弱默认凭据上线。
- Web 登录态迁移到 HttpOnly Cookie + CSRF 双提交令牌，生产默认启用 Secure Cookie，并新增后端 logout 清理 Cookie。
- 登录、公开订阅、远程订阅拉取和版本检查增加限流、缓存或认证保护，降低暴力尝试、外联消耗和公开接口指纹风险。
- 修复可信代理头与真实客户端 IP 处理，非可信代理模式使用 socket peer IP，避免 XFF 伪造影响限流。
- 加强 SSRF 防护：远程订阅和延迟测试 URL 校验公网解析，阻断 DNS rebinding、IPv4-mapped IPv6 本地地址和私网目标。

### Mihomo/Clash 导出

- 修复代理名重复导致 Mihomo 报 `duplicate name` 的问题，导出和保存模板时会自动重命名并同步更新策略组引用。
- 修复模板 `include-all-proxies: true` 没有展开为当前订阅全部节点的问题。
- 无上游模板导出时生成正确的 `PROXY` select 组和 `AUTO` url-test 组，默认测速地址切换为 `https://cp.cloudflare.com/generate_204`。
- 内置 Clash/Mihomo 模板补全常用分流规则，并移除默认 GeoIP/MMDB、DNS fallback 依赖，避免客户端因无法下载数据库而整体启动失败。
- 上游订阅导入按当前目标分组查重，允许不同分组保存相同节点；自动保存的上游模板名称包含分组名。

### 延迟测试与订阅体验

- 后台自动测速新增并发设置，批量测速改为 NDJSON 流式返回，前端可实时展示每个节点的结果。
- 手动测速撞上后台测速时提示是否停止后台任务，确认后取消后台测速并自动重试。
- Mihomo 子进程增加全局并发控制、任务单飞、冷却时间和临时文件清理，降低误操作或异常任务带来的资源压力。
- 公开订阅链接新增主题化二维码，二维码中心 `RS` 标识融入码点风格。

### 部署与运维

- Docker Compose 增加后端健康检查，前端等待后端 healthy 后启动，并修复 Nginx 启动期解析 `backend` 失败的问题。
- 修复历史迁移 `0006` 校验不稳定导致线上 SQLite 报 `Migrate(VersionMismatch(6))` 的问题，默认测速 URL 变更改由后续迁移处理。
- 更新 Windows、Linux、macOS 本地运行脚本和 Docker 部署文档，补充可信反代头、同源 CSP、后端启动失败和镜像缓存排查说明。
- 已推送 Docker Hub `latest` 镜像，并额外提供后端不可变标签 `docker.io/jojhaa/sublinkx-rs-backend:20260623-migrate-fix`。

## v0.1.0 - 2026-06-01

首个公开版本，面向多协议、多客户端订阅管理场景。

### 核心能力

- Rust/Axum 后端与 Vue 3 管理控制台。
- 默认 SQLite，支持 MySQL 8.x、容器 MySQL、本机 MySQL 和外部 MySQL。
- 节点、节点分组、订阅、订阅分组、模板和系统设置管理。
- 首次登录强制修改默认账号密码，密码使用 Argon2 哈希保存。
- 中英文界面与中英文 README。

### 节点与订阅

- 支持手动多行导入、整段 Base64 自动解码、上游订阅链接导入。
- 支持 Mihomo YAML 节点提取，并可保存上游模板用于透传导出。
- 支持订阅启用/停用、到期时间、快捷续期、分组、节点筛选和自动识别客户端链接。
- 支持详细/简约两种列表展示，适配桌面、平板和移动端。

### 协议与客户端

- 支持 Shadowsocks、VMess、VLESS、Trojan、Hysteria/Hysteria2、TUIC、WireGuard、AnyTLS 等协议方向。
- 支持 Mihomo/Clash Meta、Clash、Xray、Surge、sing-box、Quantumult X、Quantumult、Loon、Surfboard、Mellow、ClashR、SS SIP002/SIP008、SSR、SSD、Trojan URI、Mixed 等导出目标。
- 增加协议 x 客户端支持矩阵，仅在大屏展示完整表格。
- 增加转换保真检查，用于对比上游 proxy 字段和二次导出字段。

### 运维与部署

- 支持 Mihomo 内核检测、下载和真实链路延迟测试。
- 保存历史延迟、最后测速时间和不可用状态。
- 提供 Docker Compose 部署，数据映射到本地目录，并支持固定 Docker 网段。
- 提供 Windows、Linux、macOS 本地开发运行脚本。

### 默认账号

```text
admin / admin123456
```

首次登录后必须修改用户名和密码。
